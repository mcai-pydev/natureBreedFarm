# Database connection
DATABASE_URL=postgresql://username:password@localhost:5432/rabbit_breeding

# Server configuration
PORT=3001
NODE_ENV=development

# Session secret for auth (if added later)
SESSION_SECRET=your-secret-key-here